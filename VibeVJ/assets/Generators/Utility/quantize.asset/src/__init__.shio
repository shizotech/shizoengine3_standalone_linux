
entry(generator, area) {
	area.layout(nanogui.layout_vertical());
	
	in = area.controlslider("Input");
	steps = area.controlint("steps");
	out = area.controlslider("Out");
	
	in.callback([steps,out](v) {
		out.value(std.int(v * steps.value()) / std.float(steps.value()));
	});
}